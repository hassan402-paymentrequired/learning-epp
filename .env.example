# Copy to .env locally (optional). EAS builds should set this in eas.json → build.*.env
# or in EAS Secrets / project Environment Variables.
#
# Base URL for the Laravel API (with or without /api — both are normalized in src/services/api.ts).
EXPO_PUBLIC_API_BASE_URL=https://admin.stepra.com.ng
